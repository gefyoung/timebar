import { Config } from "../config";
import { EventDelegate } from "../events";
declare type Event = {
    files: string[];
};
export declare class Watcher {
    readonly onChange: EventDelegate<Event>;
    private chokidar?;
    reload(root: string, config: Config): void;
}
export {};
